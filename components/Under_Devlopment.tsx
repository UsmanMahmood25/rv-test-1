"use client";

import uds from "@/styles/UnderDevelopment.module.scss";

export default function UnderDevelopment() {
    return (
        <div className={uds.ud_container}>
            <h2 className={uds.ud_title}>Page Currently Under Development</h2>
        </div>
    );
}